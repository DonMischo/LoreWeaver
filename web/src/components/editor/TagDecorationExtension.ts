// Tag decoration extension — placeholder, no active inline tags.
// Relations are managed via the Codex system.
// Scene time is set per-scene via the time picker in the toolbar.
import { Extension } from "@tiptap/core";
export const TagDecorationExtension = Extension.create({ name: "tagDecorations" });

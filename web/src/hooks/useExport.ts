// Legacy hook kept for compatibility — the sidebar now uses ExportDialog directly.
// This hook is no longer used but kept to avoid breaking any future imports.
export function useExport() {
  return {};
}
